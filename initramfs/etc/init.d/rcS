#!/bin/sh
echo "Starting rcS..."
mount -t proc proc /proc && echo "proc mounted"
mount -t sysfs sysfs /sys && echo "sys mounted" 
mount -t devtmpfs devtmpfs /dev && echo "dev mounted"
ls -la /dev/tty* 2>/dev/null || echo "No TTY devices found"
echo "Init done, dropping to shell..."
exec /bin/sh
